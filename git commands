To connect to branch

open terminal select work directory

git clone repositorylink

After changes in any file or adding any new files

git add .

git commit -m "Comments"

git commit -a -m "Comments" (if commit did not work use this command)

git push origin master (master is a branch name, if you are in other branch use the branch name)


to get all the updated data

git pull


To check how many branches are there

git branch





Imp links
--------------------
Collections

https://chercher.tech/java/collection
